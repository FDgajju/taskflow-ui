import type React from "react";
import PageHeader from "../components/PageHeader";

const Calendar: React.FC = () => {
	return (
		<div>
			<PageHeader header="Calendar View" />
		</div>
	);
};

export default Calendar;
